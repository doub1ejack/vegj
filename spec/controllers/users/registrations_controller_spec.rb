require 'rails_helper'

RSpec.describe Users::RegistrationsController, type: :controller do
  
  describe "POST #create" do
    it "needs to check correct form parameters (I think? That the controller's job, right?)"
  end

end
